# Railway Environment Configuration
# This file contains environment variables specific to Railway deployment

# Port configuration for Railway
PORT=${PORT}

# CORS configuration to allow Railway healthcheck
ADMIN_CORS=*
AUTH_CORS=*
STORE_CORS=*

# Backend URL for Railway
BACKEND_PUBLIC_URL=https://${RAILWAY_PUBLIC_DOMAIN}

# Database URL (Railway will provide this)
DATABASE_URL=${DATABASE_URL}

# Redis URL (Railway will provide this if configured)
REDIS_URL=${REDIS_URL}

# JWT and Cookie secrets (set these in Railway dashboard)
JWT_SECRET=${JWT_SECRET}
COOKIE_SECRET=${COOKIE_SECRET}

# Worker mode
MEDUSA_WORKER_MODE=shared

# Admin configuration
# MEDUSA_DISABLE_ADMIN=true  # Descomenta para deshabilitar el admin si hay problemas